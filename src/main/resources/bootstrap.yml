spring:
  cloud:
    nacos:
      config:
        username: nacos
        password: nacos
        namespace:
        server-addr: 127.0.01:8848
        group: DEFAULT_GROUP
        file-extension: yaml
        enable_remote_sync_config: true

